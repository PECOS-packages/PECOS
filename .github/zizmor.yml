rules:
  unpinned-uses:
    config:
      policies:
        # Existing workflows use version tags for most actions. Keep this
        # blocking scan focused on executable workflow risks for now; full
        # SHA pinning can be promoted separately.
        "*": ref-pin
