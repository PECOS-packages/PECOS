# pecos-cli

PECOS CLI for dependency management, CUDA-aware builds, and system inspection.

Install with `cargo install --path crates/pecos-cli` or `just install-cli`.

The binary is named `pecos`. Run `pecos --help` for usage.
