[project]
name = "zlup-docs"
version = "0.1.0"
description = "Zlup documentation"
requires-python = ">=3.10"
dependencies = [
    "mkdocs>=1.6",
    "mkdocs-material>=9.5",
]

[project.optional-dependencies]
dev = [
    "mkdocs-minify-plugin",
]
