[package]
name = "pecos-go-ffi"
version = "0.1.0-dev0"
edition.workspace = true
authors.workspace = true
homepage.workspace = true
repository.workspace = true
license.workspace = true
keywords.workspace = true
categories.workspace = true
description = "C FFI bindings for PECOS Go package"
publish = false

[lib]
name = "pecos_go"
crate-type = ["cdylib"]

[dependencies]
# Use pecos with only core feature for minimal dependencies
# The Go wrapper currently only needs QubitId from pecos-core
pecos = { workspace = true, default-features = false, features = ["core"] }

[lints]
workspace = true
