from pecos.qeclib.fqc import decoders, frame_rots, gates_sq, gates_tq, inits, meas, syn_extract


class FiveQubitCode:
    decoding = decoders
    frame_rots = frame_rots
    gates_sq = gates_sq
    gates_tq = gates_tq
    inits = inits
    meas = meas
    syn_extract = syn_extract
