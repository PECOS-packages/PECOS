[project]
name = "pecos-selene-qulacs"
version = "0.8.0.dev3"
requires-python = ">=3.10"
description = "PECOS Qulacs simulator plugin for the Selene quantum emulator"
readme = "README.md"
license = "Apache-2.0"
dependencies = [
    "selene-core>=0.2",
]

[project.optional-dependencies]
test = [
    "pytest>=7",
    "selene-sim>=0.2",
    "guppylang>=0.14",
]

[project.urls]
homepage = "https://pecos.io"
repository = "https://github.com/PECOS-packages/PECOS"

# Attribution: This plugin wraps Qulacs, a high-performance quantum simulator.
# Qulacs is developed by the Qulacs team and is available at:
# https://github.com/qulacs/qulacs
# Qulacs is licensed under the MIT License.

[build-system]
requires = ["hatchling", "packaging"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["python/pecos_selene_qulacs"]

[tool.hatch.build.hooks.custom]
path = "hatch_build.py"

[tool.uv]
cache-keys = [
    { file = "src/**/*.rs" },
    { file = "Cargo.toml" },
]
